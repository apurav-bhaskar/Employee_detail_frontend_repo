import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    formStyle: {
      display: "grid",
      justifyContent: "center",
      padding: "130px",
    },
    linkStyle: {
      textDecoration: "none",
    },
  }));